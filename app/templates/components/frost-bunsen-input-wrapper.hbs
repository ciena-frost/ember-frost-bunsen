{{#if shouldRender}}
  {{component inputName
    bunsenId=bunsenId
    cellConfig=cellConfig
    errorMessage=errorMessage
    model=model
    onChange=onChange
    readOnly=readOnly
    required=required
    store=store
    value=value
  }}
{{/if}}
