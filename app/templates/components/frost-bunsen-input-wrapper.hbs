{{#if shouldRender}}
  {{component inputName
    bunsenId=bunsenId
    bunsenModel=bunsenModel
    bunsenStore=bunsenStore
    cellConfig=cellConfig
    errorMessage=errorMessage
    onChange=onChange
    readOnly=readOnly
    required=required
    value=value
  }}
{{/if}}
