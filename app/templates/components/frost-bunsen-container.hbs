{{#if config.label}}
  {{#frost-bunsen-section
    className="model-container"
    collapsible=config.collapsible
    instructions=config.instructions
    required=isRequired
    title=config.label
  }}
    <div class={{config.className}}>
      {{#each config.rows as |row index|}}
        {{frost-bunsen-row
          bunsenId=bunsenId
          bunsenModel=bunsenModel
          bunsenStore=bunsenStore
          cellConfigs=row
          defaultClassName=config.defaultClassName
          errors=errors
          onChange=onChange
          readOnly=readOnly
          value=value
        }}
      {{/each}}
    </div>
  {{/frost-bunsen-section}}
{{else}}
  <div class={{config.className}}>
    {{#each config.rows as |row index|}}
      {{frost-bunsen-row
        bunsenId=bunsenId
        bunsenModel=bunsenModel
        bunsenStore=bunsenStore
        cellConfigs=row
        errors=errors
        defaultClassName=config.defaultClassName
        onChange=onChange
        readOnly=readOnly
        value=value
      }}
    {{/each}}
  </div>
{{/if}}
