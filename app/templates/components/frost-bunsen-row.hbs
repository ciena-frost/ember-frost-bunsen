{{#each cellConfigs as |config index|}}
  {{frost-bunsen-cell
    bunsenId=bunsenId
    config=config
    class=config.className
    defaultClassName=defaultClassName
    errors=errors
    id=(concat elementId '-' index)
    model=model
    onChange=onChange
    readOnly=readOnly
    store=store
    value=value
  }}
{{/each}}
