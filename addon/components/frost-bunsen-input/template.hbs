{{#if shouldRender}}
  {{component inputName
    bunsenId=bunsenId
    cellConfig=cellConfig
    initialValue=initialValue
    model=model
    onChange=onChange
    readOnly=readOnly
    required=required
    store=store
    reduxStore=reduxStore
  }}
{{/if}}
