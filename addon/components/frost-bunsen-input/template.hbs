{{#if shouldRender}}
  {{component inputName
    bunsenId=bunsenId
    cellConfig=cellConfig
    initialValue=initialValue
    model=model
    on-change=on-change
    readOnly=readOnly
    required=required
    store=store
  }}
{{/if}}
