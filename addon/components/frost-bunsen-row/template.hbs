{{#each cellConfigs as |config|}}
  {{frost-bunsen-cell
    bunsenId=bunsenId
    config=config
    class=config.className
    defaultClassName=defaultClassName
    model=model
    on-change=on-change
    readOnly=readOnly
    store=store
  }}
{{/each}}
