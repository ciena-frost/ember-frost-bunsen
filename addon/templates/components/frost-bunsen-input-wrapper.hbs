{{#if shouldRender}}
  {{component inputName
    bunsenId=bunsenId
    bunsenModel=bunsenModel
    bunsenStore=bunsenStore
    cellConfig=cellConfig
    errorMessage=errorMessage
    formHook=formHook
    onChange=onChange
    readOnly=readOnly
    required=required
    value=value
  }}
{{/if}}
